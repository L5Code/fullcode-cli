const router = require('koa-router')();

const { apiPrePath } = require('../config/index');

router.prefix(apiPrePath + '<%=data.lowerName %>');

/**
* get请求示例，一般用于查询表数据，不带更新和增加功能
*/
router.get('/getTest', async (ctx) => {
	let { CUST_CODE } = ctx.query;
	const sql = `
	select * from Data0010 where CUST_CODE = '${CUST_CODE}'
	`;
	let res_data = await Select_SQL(area, sql);
	return ctx.body = {
		status: 200,
		msg: '获取记录成功',
		res: res_data
	};
});

module.exports = router;